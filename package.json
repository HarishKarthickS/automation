{
  "name": "automation-saas-monorepo",
  "private": true,
  "version": "1.0.0",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.6.5",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}

